{
  "name": "ts-docker",
  "version": "1.0.0",
  "description": "testing simple node-app with docker in typescript",
  "scripts": {
    "test": " echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon --watch 'src/**' --ext 'ts,json' --exec ts-node './src/server.ts'",
    "build": "rm -rf dist && tsc",
    "start": "cross-env NODE_ENV=production node ./dist/server.js"
  },
  "author": "dinta",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/node": "^17.0.15",
    "nodemon": "^2.0.15",
    "ts-node": "^10.4.0",
    "typescript": "^4.5.5"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "cross-env": "^7.0.3",
    "dotenv": "^16.0.0",
    "express": "^4.17.2"
  }
}